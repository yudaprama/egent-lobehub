name: Build and Release

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GOPRIVATE: github.com/kawai-network/*
    steps:
      - uses: actions/checkout@v4

      - name: Authenticate git for private Go modules
        run: git config --global url."https://x-access-token:${{ secrets.PRIVATE_REPO_TOKEN }}@github.com/kawai-network/".insteadOf "https://github.com/kawai-network/"

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache: true

      - name: Build linux-amd64
        run: |
          mkdir -p dist
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -ldflags "-s -w" -o dist/egent-lobehub-linux-amd64 .

      - name: Build linux-arm64
        run: CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go build -trimpath -ldflags "-s -w" -o dist/egent-lobehub-linux-arm64 .

      - name: Build darwin-amd64
        run: CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -trimpath -ldflags "-s -w" -o dist/egent-lobehub-darwin-amd64 .

      - name: Build darwin-arm64
        run: CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -trimpath -ldflags "-s -w" -o dist/egent-lobehub-darwin-arm64 .

      - name: Checksums
        run: sha256sum dist/* > dist/CHECKSUMS.txt

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: Release v${{ github.run_number }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: dist/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
